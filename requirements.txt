# Core crawling
aiohttp>=3.9
beautifulsoup4>=4.12
lxml>=5.0

# File-type text extraction (optional but recommended)
pypdf>=4.0
python-docx>=1.1

# Web search UI
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1

# Config
PyYAML>=6.0

# Optional: JavaScript rendering (enable render_js). Heavy; install separately:
#   pip install playwright && playwright install chromium
# playwright>=1.43
